# 安全策略

## 支持范围

| 版本 | 状态 |
| --- | --- |
| `main` / 最新发布版 | 接受安全修复 |
| 更早的预览版本 | 不保证回溯修复 |

CodeDoctor 仍处于开发者预览阶段。发布稳定版本后，本表会改为明确的版本支持窗口。

## 私下报告漏洞

不要公开创建包含漏洞细节、真实密钥或私有源码的 Issue。仓库发布到 GitHub 后，请使用仓库 **Security → Advisories → Report a vulnerability** 的 Private vulnerability reporting；在该功能启用前，请通过维护者在仓库主页公布的非公开联系方式报告。

报告请包含：

- 受影响版本、包、入口和前置条件。
- 最小、已脱敏的复现步骤或测试项目。
- 可观察影响及可能的攻击路径。
- 已尝试的缓解方式。
- 是否已在其他地方公开披露。

请不要附真实用户项目、生产日志、API Key、访问令牌或个人信息。

## 响应目标

- 3 个工作日内确认收到报告。
- 7 个工作日内给出初步影响判断和后续安排。
- 修复时间根据严重程度、兼容性和披露风险确定。

这些时间是响应目标而非服务等级承诺。修复完成后，维护者会协调发布、安全公告和致谢；未经报告者同意，不公开其身份。

## 重点安全边界

- 浏览器文件夹导入会在本机读取用户选择的文件；未经明确操作，不应上传项目源码。
- AI 解释会把当前问题的必要证据发送给所配置的模型服务，用户应先检查内容是否可外发。
- API Key 不得进入仓库、报告、遥测或持久化日志；一旦暴露应立即轮换。
- 静态扫描结果不是漏洞不存在的证明，依赖检查也不包含完整漏洞数据库。

## 密钥意外暴露

立即吊销或轮换凭据，删除工作树中的值，并检查 Git 历史、构建日志、发布产物和缓存。仅从最新文件删除密钥不能消除历史泄露。
