# 参与贡献

感谢你改进 CodeDoctor。这个项目会读取陌生源码并给出评分，因此比一般界面项目更重视证据、误报控制、隐私和可复现验证。

## 开始之前

- 缺陷请使用 GitHub 的“缺陷报告”表单，并提供最小、已脱敏的复现项目。
- 新语言、框架或评分规则建议先说明适用项目、可靠证据、误报边界和性能成本。
- 安全漏洞不要公开创建 Issue，请按 [SECURITY.md](./SECURITY.md) 私下报告。
- 不要上传公司源码、真实 API Key、`.env`、日志凭据或未经授权的数据集。

## 本地开发

要求 Node.js 22+ 和 pnpm 9+：

```bash
pnpm install --frozen-lockfile
pnpm dev
```

提交前运行：

```bash
pnpm check
pnpm scan .
```

`pnpm check` 会运行全部测试并构建所有工作区包。自扫分数只能帮助发现问题，不能替代测试和人工审查。

## 工程边界

- `apps/web`：浏览器工作台和浏览器内扫描入口。
- `apps/cli`：本地 CLI 与报告落盘。
- `packages/detectors`：项目、语言、框架和文件范围识别。
- `packages/analyzers`：规则分析器，必须输出可追踪证据。
- `packages/scoring`：固定权重与可解释扣分。
- `packages/report`：JSON/Markdown 报告。
- `packages/ai`：可选模型解释，不参与基础静态结论。

详细边界和信任模型见 [docs/ARCHITECTURE.md](./docs/ARCHITECTURE.md)。

## 分支与提交

1. 从最新 `main` 创建短生命周期分支。
2. 一次 PR 聚焦一个用户问题或一组紧密相关的规则。
3. 提交信息使用清晰的命令式描述；依赖和 CI 更新可使用 `deps:`、`ci:` 前缀。
4. 不提交 `dist`、`node_modules`、`.codedoctor`、`work`、本地环境文件和真实扫描样本。

## 分析规则验收标准

新增或修改规则必须同时满足：

- 有明确、可引用的文件或内容证据，不能仅凭项目名字猜测。
- 至少包含一个命中测试和一个反误报测试。
- 正确区分生产源码、测试、夹具、文档、依赖和生成物。
- 给出适用项目、置信度、修复建议和误报风险。
- 浏览器与 CLI 能力不一致时，必须在覆盖说明中明确披露。
- 不允许通过扩大 `.codedoctorignore` 或降低规则强度来修饰自扫分数。

## 网页改动

- 保持用户界面中文，不在页面堆叠使用说明。
- 验证总览、问题、AI、报告四个视图及文件夹导入主流程。
- 检查桌面和移动端是否存在溢出、遮挡、不可点击或控制台错误。
- 不把 API Key 写入源码、URL、持久化日志或构建产物。

## Pull Request

PR 应说明问题、实现边界、验证命令、评分变化和剩余风险。CI 必须通过；涉及行为变化时应更新 README 或 [CHANGELOG.md](./CHANGELOG.md)。维护者可以要求拆分无关重构、增加反误报测试或补充脱敏证据。
